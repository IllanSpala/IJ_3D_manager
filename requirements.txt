
# IJ 3D Manager — Python dependencies
# Install with: pip install -r requirements.txt  (inside the active venv)
 
# Modern, themed Tkinter widgets — dark mode, rounded corners, CTkImage.
customtkinter>=5.2.2
 
# Image processing: open, resize, convert to CTkImage.
# Pillow 10+ is required for Image.Resampling.LANCZOS (replaces the
# deprecated Image.ANTIALIAS alias removed in Pillow 10).
Pillow>=10.0.0
 